# 🔧 Инструкция по исправлению проблемы с сохранением названий блюд

## ✅ Что было исправлено

Миграция базы данных была успешно выполнена. Поля `name_ru`, `name_en`, `name_kk` добавлены в таблицу `dishes`.

## 🚀 Что нужно сделать сейчас

### 1. Перезапустите backend сервер

**ВАЖНО:** После применения миграции необходимо перезапустить backend сервер!

```bash
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
cd backend
npm start
```

### 2. Очистите кэш браузера

После перезапуска сервера:
1. Откройте админ-панель
2. Нажмите `Ctrl + Shift + R` (или `Cmd + Shift + R` на Mac) для жесткой перезагрузки
3. Или очистите кэш в настройках браузера

### 3. Проверьте работу

1. Зайдите в админ-панель → "Управление блюдами"
2. Создайте новое блюдо или отредактируйте существующее
3. Заполните названия на трех языках:
   - **Название на русском** (обязательно) ⭐
   - **Название на английском** (опционально)
   - **Название на казахском** (опционально)
4. Нажмите "Сохранить"
5. Проверьте, что блюдо сохранилось
6. Переключите язык на сайте и убедитесь, что названия меняются

## 🐛 Отладка (если проблема остается)

Откройте консоль браузера (F12) и проверьте:
1. Какие данные отправляются при сохранении (должны быть `name_ru`, `name_en`, `name_kk`)
2. Какой ответ приходит от сервера
3. Есть ли ошибки в консоли

## 📝 Структура данных

Теперь блюдо должно иметь следующие поля:
```json
{
  "name": "Плов",           // Основное название (копия name_ru)
  "name_ru": "Плов",        // Название на русском
  "name_en": "Pilaf",       // Название на английском
  "name_kk": "Палау",       // Название на казахском
  "description_ru": "...",
  "description_en": "...",
  "description_kk": "...",
  "price": 2500,
  // ... остальные поля
}
```

## ✨ Готово!

После перезапуска сервера все должно работать корректно!
