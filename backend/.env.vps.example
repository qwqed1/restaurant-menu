# Конфигурация для VPS сервера
# Скопируйте этот файл в .env и заполните своими данными

# Настройки сервера
PORT=3001
NODE_ENV=production
SERVER_HOST=0.0.0.0

# Настройки базы данных PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=halalhall
DB_USER=halalhall
DB_PASSWORD=ваш_надежный_пароль_здесь

# JWT секретный ключ (ОБЯЗАТЕЛЬНО ИЗМЕНИТЕ!)
# Сгенерируйте случайную строку: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=замените_на_случайную_строку_минимум_32_символа

# CORS настройки (IP вашего VPS)
ALLOWED_ORIGINS=http://194.32.142.53,http://localhost:5173
