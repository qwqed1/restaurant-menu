# Настройка локальной сети для ресторана

## 🏠 Архитектура системы

```
Компьютер (Windows) - Сервер базы данных
    ↓ WiFi роутер (локальная сеть)
    ├── Планшеты клиентов (10 шт) - Просмотр меню
    └── Планшет администратора (1 шт) - Управление меню
```

---

## 📋 Что нужно:

1. **Один компьютер** (Windows) - будет сервером
2. **WiFi роутер** - для локальной сети
3. **Планшеты** (10-15 шт) - с установленным APK
4. **Все устройства** подключены к одной WiFi сети

---

## 🔧 Шаг 1: Настройка компьютера (сервера)

### 1.1 Узнайте IP адрес компьютера

Откройте PowerShell и выполните:
```powershell
ipconfig
```

Найдите строку **IPv4 Address**, например: `192.168.1.100`

### 1.2 Настройте файл .env.production

Откройте файл `frontend\.env.production` и замените IP адрес на ваш:

```env
VITE_API_URL=http://192.168.1.100:3001
VITE_USE_API=true
```

**Замените `192.168.1.100` на IP адрес вашего компьютера!**

### 1.3 Настройте backend

Откройте файл `backend\.env` и убедитесь, что настройки правильные:

```env
PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_NAME=halalhall
DB_USER=postgres
DB_PASSWORD=ваш_пароль
JWT_SECRET=ваш_секретный_ключ
```

### 1.4 Откройте порт в брандмауэре Windows

1. Откройте **Панель управления → Брандмауэр Windows**
2. Нажмите **Дополнительные параметры**
3. Выберите **Правила для входящих подключений**
4. Нажмите **Создать правило**
5. Выберите **Порт** → Далее
6. Выберите **TCP**, укажите порт **3001** → Далее
7. Выберите **Разрешить подключение** → Далее
8. Отметьте все профили → Далее
9. Назовите правило "Halal Hall Server" → Готово

---

## 🚀 Шаг 2: Запуск сервера

### 2.1 Установите PostgreSQL (если не установлен)

Скачайте и установите PostgreSQL с официального сайта:
https://www.postgresql.org/download/windows/

### 2.2 Запустите backend сервер

```powershell
cd backend
npm install
npm run init
npm start
```

Сервер должен запуститься на порту 3001.

### 2.3 Проверьте работу сервера

Откройте браузер и перейдите:
```
http://localhost:3001/api/categories
```

Должен вернуться JSON с категориями.

---

## 📱 Шаг 3: Сборка APK для планшетов

### 3.1 Соберите приложение

```powershell
cd frontend
npm install
npm run build
npx cap sync
npx cap open android
```

### 3.2 В Android Studio

1. Дождитесь синхронизации Gradle
2. **Build → Build Bundle(s) / APK(s) → Build APK(s)**
3. APK будет в: `frontend\android\app\build\outputs\apk\debug\app-debug.apk`

### 3.3 Установите APK на все планшеты

Скопируйте `app-debug.apk` на все планшеты и установите.

---

## ✅ Шаг 4: Проверка работы

### 4.1 Проверьте подключение с планшета

На планшете откройте браузер и перейдите:
```
http://192.168.1.100:3001/api/categories
```

Если видите JSON - всё работает!

### 4.2 Запустите приложение

1. Откройте приложение на планшете
2. Выберите **Клиент** или **Администратор**
3. Меню должно загрузиться с сервера

---

## 🔄 Автозапуск сервера при включении компьютера

### Создайте bat-файл для автозапуска

Создайте файл `start-server.bat` в папке backend:

```batch
@echo off
cd /d "C:\Users\Admin\Documents\Halal hail\vostoshny dvor\backend"
start /min cmd /c "npm start"
```

### Добавьте в автозагрузку Windows

1. Нажмите `Win + R`
2. Введите: `shell:startup`
3. Скопируйте туда файл `start-server.bat`

Теперь сервер будет запускаться автоматически при включении компьютера!

---

## 🛠️ Решение проблем

### Планшеты не подключаются к серверу

1. **Проверьте WiFi**: Все устройства в одной сети?
2. **Проверьте IP**: Правильный ли IP в `.env.production`?
3. **Проверьте брандмауэр**: Открыт ли порт 3001?
4. **Пингуйте сервер**: С планшета выполните `ping 192.168.1.100`

### Сервер не запускается

1. **PostgreSQL запущен?** Проверьте в службах Windows
2. **Правильные данные в .env?** Проверьте пароль БД
3. **Порт занят?** Измените PORT в .env на другой (например, 3002)

### Меню не обновляется

1. Проверьте интернет на сервере (для загрузки изображений)
2. Перезапустите сервер: `npm start`
3. Очистите кэш на планшетах

---

## 💡 Рекомендации

1. **Используйте статический IP** для компьютера-сервера
2. **Резервное копирование БД** раз в неделю
3. **UPS (ИБП)** для компьютера-сервера
4. **Качественный WiFi роутер** для стабильной работы
5. **Держите компьютер включенным** во время работы ресторана

---

## 📊 Преимущества локальной сети

✅ **Не нужен интернет** - работает без внешнего подключения  
✅ **Быстрая скорость** - всё в локальной сети  
✅ **Безопасность** - данные не выходят за пределы ресторана  
✅ **Нет затрат на хостинг** - всё работает локально  
✅ **Полный контроль** - вы управляете всем  

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Все устройства в одной WiFi сети
2. Сервер запущен на компьютере
3. IP адрес правильный в настройках
4. Порт 3001 открыт в брандмауэре
