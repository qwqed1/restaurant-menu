# Мобильная адаптация меню ресторана (Portrait Mode)

## Обзор изменений

Приложение теперь полностью адаптировано для мобильных телефонов в вертикальной ориентации (portrait mode). Все компоненты оптимизированы для экранов от 320px до 768px.

---

## 🏠 Главная страница (HomePage)

### Изменения:
- **Логотип**: Уменьшен с 320px до 192px на мобильных (w-48 → w-80)
- **Заголовок**: Адаптивный размер текста (text-4xl → text-7xl)
- **Кнопки навигации**: 
  - Вертикальное расположение на мобильных
  - Полная ширина с максимумом 448px (max-w-md)
  - Уменьшенные отступы и иконки
  - Текст кнопок центрирован
- **Селектор языка**: Компактная версия в правом верхнем углу

### Breakpoints:
- Mobile: < 768px (вертикальный стек)
- Tablet: ≥ 768px (горизонтальный ряд)

---

## 📋 Страница меню (App.jsx)

### Главные изменения:

#### 1. **Карусель категорий** (главный экран)
- Изображение блюда: 192px → 256px (w-48 → w-64)
- Название категории: text-2xl → text-3xl
- Кнопка "Перейти в меню": уменьшенные отступы и border
- Центрированный контент с максимальной шириной

#### 2. **Навигация по категориям**
**Desktop (≥768px):**
- Боковая панель справа (Sidebar)
- Фиксированная ширина 256px

**Mobile (<768px):**
- Нижняя панель навигации (Bottom Navigation)
- Выдвижной drawer с категориями
- Кнопка переключения с текущей категорией
- Backdrop blur эффект
- Максимальная высота drawer: 256px (с прокруткой)

#### 3. **Список блюд**
- Добавлен нижний отступ (pb-20) для мобильных, чтобы контент не перекрывался навигацией
- Уменьшенные отступы контейнера (px-3 вместо px-6)

---

## 🎨 Компоненты

### Header
- **Padding**: px-3 py-3 (mobile) → px-6 py-5 (desktop)
- **Иконки**: w-4 h-4 (mobile) → w-5 h-5 (desktop)
- **Заголовок**: text-lg (mobile) → text-2xl (desktop)
- **Truncate**: Обрезка длинных названий категорий

### DishCard
- **Изображение**: w-20 h-20 (mobile) → w-32 h-32 (desktop)
- **Название**: text-base (mobile) → text-lg (desktop)
- **Описание**: text-xs (mobile) → text-sm (desktop)
- **Кнопка "Инфо"**: Сокращенный текст на мобильных
- **Цена**: 
  - Круг: w-16 h-16 (mobile) → w-24 h-24 (desktop)
  - Текст: text-base (mobile) → text-2xl (desktop)
- **Отступы**: Уменьшены gaps между элементами

### DishModal
- **Layout**: 
  - Mobile: Одна колонка (grid-cols-1)
  - Desktop: Две колонки (grid-cols-2)
- **Изображение**: w-48 h-48 (mobile) → w-64 h-64 (tablet) → w-80 h-80 (desktop)
- **Padding**: Уменьшен на всех уровнях для мобильных
- **Текст**: Адаптивные размеры для всех элементов
- **Кнопка закрытия**: Меньший размер и отступы
- **Прокрутка**: max-h-[95vh] с overflow-y-auto

### LanguageSelector
- **Кнопка**: 
  - Компактная на мобильных (px-2 py-1.5)
  - Показывает только флаг на очень маленьких экранах
  - Полное название на sm и выше
- **Dropdown**: 
  - Ширина: w-44 (mobile) → w-52 (desktop)
  - Уменьшенные отступы в списке

---

## 📱 Responsive Breakpoints

```css
/* Tailwind breakpoints используемые в проекте */
sm:  640px  /* Small devices */
md:  768px  /* Medium devices (tablets) */
lg:  1024px /* Large devices */
```

### Стратегия:
- **Mobile-first**: Базовые стили для мобильных
- **Progressive enhancement**: Улучшения для больших экранов через md: и lg: префиксы

---

## 🎯 Ключевые особенности мобильной версии

### 1. **Нижняя навигация**
- Фиксированная позиция внизу экрана
- Backdrop blur для эффекта стекла
- Анимированный chevron при открытии/закрытии
- Автоматическое закрытие при выборе категории

### 2. **Оптимизация пространства**
- Уменьшенные изображения и отступы
- Компактные кнопки и иконки
- Truncate для длинных текстов
- Эффективное использование вертикального пространства

### 3. **Touch-friendly**
- Увеличенные области нажатия
- Кнопки минимум 44x44px (Apple HIG)
- Достаточные отступы между элементами
- Hover эффекты работают как active на touch

### 4. **Производительность**
- Условный рендеринг (hidden/block) вместо display: none
- Оптимизированные анимации
- Lazy loading изображений (встроено в браузер)

---

## 🔧 Технические детали

### CSS классы для адаптивности:
```jsx
// Пример из DishCard
className="w-20 h-20 md:w-32 md:h-32"  // Размер изображения
className="text-base md:text-lg"       // Размер текста
className="px-3 md:px-6"               // Padding
className="hidden md:block"            // Скрыть на мобильных
className="md:hidden"                  // Показать только на мобильных
```

### Мобильная навигация:
```jsx
// Состояние drawer
const [isMobileCategoriesOpen, setIsMobileCategoriesOpen] = useState(false);

// Условный рендеринг
<div className="md:hidden">  // Только на мобильных
<div className="hidden md:block">  // Только на desktop
```

---

## 📊 Тестирование

### Рекомендуемые устройства для тестирования:
- iPhone SE (375x667)
- iPhone 12/13 (390x844)
- Samsung Galaxy S21 (360x800)
- Google Pixel 5 (393x851)

### Chrome DevTools:
1. F12 → Toggle device toolbar
2. Выбрать мобильное устройство
3. Протестировать portrait и landscape
4. Проверить touch events

---

## ✅ Что работает на мобильных

✓ Главная страница с вертикальными кнопками  
✓ Карусель категорий с адаптивными изображениями  
✓ Нижняя навигация с drawer для категорий  
✓ Список блюд с компактными карточками  
✓ Модальное окно блюда с вертикальным layout  
✓ Селектор языка с компактной кнопкой  
✓ Все анимации и переходы  
✓ Touch-friendly интерфейс  

---

## 🚀 Запуск и тестирование

```bash
# Запустить frontend
cd frontend
npm run dev

# Открыть в браузере
http://localhost:5173

# Для тестирования на реальном устройстве:
# 1. Узнать IP компьютера: ipconfig (Windows) / ifconfig (Mac/Linux)
# 2. Открыть на телефоне: http://[YOUR_IP]:5173
```

---

## 📝 Примечания

- Все размеры оптимизированы для экранов 320px-768px
- Сохранена цветовая схема: зеленый (#0D3B2F) + золотой (#cab57c)
- Все тексты локализованы (RU/EN/KK)
- Цены всегда в тенге (₸)
- Адаптация не влияет на admin панель (остается desktop-only)
