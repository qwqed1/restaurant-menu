# 🚀 Инструкция по деплою исправления

## Проблема
Ошибка 401 (Unauthorized) при сохранении блюд в админ-панели - токен авторизации не передавался в запросах.

## Что исправлено
✅ Добавлена передача токена авторизации в заголовках при создании/обновлении блюд

## Как задеплоить

### 1. Соберите frontend локально
```bash
cd frontend
npm run build
```

### 2. Загрузите на сервер
```bash
# На вашем компьютере
scp -r dist/* ubuntu@your-server:/home/ubuntu/frontend_update/
```

Или через FileZilla/WinSCP загрузите папку `frontend/dist/*` в `/home/ubuntu/frontend_update/`

### 3. На сервере обновите frontend
```bash
sudo -i
cp -r /var/www/halalhall /var/www/halalhall.backup.$(date +%Y%m%d_%H%M%S)
rm -rf /var/www/halalhall/*
cp -r /home/ubuntu/frontend_update/* /var/www/halalhall/
chown -R www-data:www-data /var/www/halalhall
chmod -R 755 /var/www/halalhall
systemctl restart nginx
rm -rf /home/ubuntu/frontend_update
exit
```

### 4. Проверьте работу
1. Очистите кэш браузера (`Ctrl + Shift + R`)
2. Зайдите в админ-панель
3. Отредактируйте блюдо
4. Заполните названия на трех языках
5. Сохраните - ошибка 401 должна исчезнуть

## Альтернатива (если не хотите пересобирать)

Можете отредактировать файл напрямую на сервере:

```bash
sudo nano /var/www/halalhall/assets/index-[hash].js
```

Найдите строку с `axios.put` и `axios.post` для dishes и добавьте config с токеном.

Но лучше пересобрать и загрузить правильную версию! ✨
